* text=auto eol=lf
*.ps1 text eol=crlf
# cmd.exe неверно разбирает многострочные блоки в .bat, сохранённом с концами строк LF.
*.bat text eol=crlf
# Хуки git запускает через bash, а тому нужен LF на любой платформе.
.githooks/** text eol=lf
*.png binary
*.jpg binary
