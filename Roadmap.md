# Roadmap.md — три дня до открытой беты

Флаги: ✅ готово · 🔄 в работе · ⬜ не начато · ⛔ блокер.
Флаг ставится только после зелёного `scripts/ci.ps1`. Документы правятся тем же
коммитом, что и код (`Claude.md`, правило 10).

**Готовность ОБТ:** локальный сервер (`Start.bat`) держит сутки без падений;
бот, группа и канал связаны; из группы работают профиль, лавка и обмен; в канал
уходят бродкасты; арена принимает бой; PvE проходится с 1 по 30 уровень.

## День 0 — что уже есть ✅

- ✅ 0.1 Архитектура, гейт качества, Docker, миграции, healthcheck, ADR 1–5.
- ✅ 0.2 Контент: 16 рас, 8 классов, 64 черты, 128 умений, 24 архетипа врагов,
  15 городов × 5 локаций на уровни 1–300, валидация на старте.
- ✅ 0.3 Создание персонажа, главное меню, мир, город, локации, узлы.
- ✅ 0.4 PvE-бой: панель 6+3+1, 1–3 врага, эффекты, откаты, без таймеров.
- ✅ 0.5 Инвентарь, лавка города, настройки доступности, страничные списки.
- ✅ 0.6 Модуль бродкастов `presentation/telegram/broadcast.py`, CLI
  `scripts/broadcast.py`, тесты `tests/presentation/test_broadcast.py`.

## День 1 — бой, умения, тексты

- ⬜ 1.1 Уникальные механики боя (`domain/rules/combat.py`, без новых кнопок):
  - ⬜ 1.1.1 **Намерение**: враг словами объявляет следующее действие.
  - ⬜ 1.1.2 **След**: у каждого действия тег — натиск, оборона, точность;
    два одинаковых подряд дают разгон, три разных — перелом (свободный ход).
  - ⬜ 1.1.3 **Брешь**: контр-тег против объявленного намерения снимает броню.
  - ⬜ 1.1.4 Строка состояния в экране боя: «След: натиск, 2 подряд».
  - ⬜ 1.1.5 Тесты на детерминизм и на баланс порогов, покрытие домена ≥ 90 %.
- ⬜ 1.2 Экран «Умения»: набор 6 из 8 и 3 из 6, ранги, грани на 3-м ранге,
  переучивание у наставника, экран в `all_screens`.
- ⬜ 1.3 Квесты акта I (1–30) по `Narrative.md`: формат диалога, отказ, цена,
  журнал заданий, хранение прогресса.
- ⬜ 1.4 Переименование рас и классов в велларские народы и ремёсла по
  `Narrative.md`, раздел 2: правятся `name`/`description`, идентификаторы
  остаются. Тем же проходом — имена умений, врагов и предметов, где они ещё
  звучат как общий фэнтези-набор.
- ⬜ 1.5 Таверна, наставник, банк, данжи — вместо заглушек.

## День 2 — люди: группа, канал, обмен

- ✅ 2.1 Связка бот ↔ группа ↔ канал: канал «Веллар_новости» и привязанная
  группа «Веллар» прописаны в `.env` числовыми ID, бот администратор в обоих,
  боевой пост в канал доставлен.
- ✅ 2.2 Команды в группе — только ответом (reply) на чужое сообщение, цель
  операции определяется тем, кому отвечают (`Narrative.md`, раздел 9):
  - ✅ 2.2.0 Грамматика и разбор: `domain/rules/group_commands.py`, всё
    неопознанное — молчание.
  - ✅ 2.2.1 `профиль` — персонаж того, кому отвечают, одним сообщением; золото
    и сумка не показываются.
  - ✅ 2.2.2 `продать <цена> <предмет>` и `купить <цена> <предмет>` —
    предложение цели; вещь ищется в сумке той стороны, которая её отдаёт.
  - ✅ 2.2.3 `передать <N> <предмет>` и `передать <N> золота` — сразу, без
    подтверждения получателя.
  - ✅ 2.2.4 Подтверждение: `selective`-клавиатура «Принять N» и «Отказ N»
    видна только цели, кнопка равна текстовой команде; чужое нажатие получает
    отказ; предложение живёт 5 минут и ничего не резервирует.
  - ✅ 2.2.5 Сообщения бота в группе удаляются через 5 минут (в личке — никогда).
  - ✅ 2.2.6 Антиспам: 6 команд в минуту на игрока, предупреждение один раз;
    экраны игры отвечают только в личке.
- ✅ 2.3 Сделки в базе: таблица `trades` и миграция `0002`, эскроу стороны автора
  (вещь продавца или золото покупателя уходят при публикации и возвращаются при
  отказе, просрочке и подметании), пошлина 5 процентов как единственный отток
  золота (`domain/rules/economy.trade_tax`), журнал сделок с их исходом. Закрытие
  строки атомарно, поэтому два «Принять» рассчитывают сделку один раз.
- ✅ 2.4 Канал: новости и чейнджлог как новость для игроков (`Narrative.md`,
  раздел 8). Обновления живут в `content/changelog.toml` и уходят в канал
  командой `scripts/broadcast.py --changelog latest`; пост, написанный языком
  разработчика (модуль, коммит, баг, API), отклоняется на рендере. Действия
  игроков в канал не уходят — только в группу, и это держит тест: ни один
  модуль из `handlers`, `flows`, `screens` не зовёт `announce`.
- ✅ 2.5 Приватность: `скрыть профиль` и `открыть профиль` — про себя, без ответа
  кому-либо; `блок` и `разблок` ответом — чёрный список. Оба лежат на аккаунте, а
  не на персонаже (миграция `0003`: колонка `users.show_profile`, таблица
  `blocks`), поэтому вторым персонажем список не обойти. Блок закрывает пару в
  обе стороны и отменяет висящее предложение, вернув отложенное автору.

## День 3 — арена, эндгейм, запуск

- ⬜ 3.1 Арена «Долговой круг»:
  - ⬜ 3.1.1 Очередь по уровню, три круга, ставка, награда.
  - ⬜ 3.1.2 Таймер хода 60 с **только здесь**; автодействие из настроек.
  - ⬜ 3.1.3 Разрыв связи, отказ, ничья, техническое поражение.
  - ⬜ 3.1.4 Итог участникам и в группу, таблица сезона.
- ⬜ 3.2 Эндгейм: Оборот на 300 уровне, Печать Палаты, вопрос цикла и его
  подсчёт (`Narrative.md`, раздел 6).
- ⬜ 3.3 Эксплуатация: бэкап Postgres по расписанию и проверка восстановления,
  лимит Telegram 30 сообщений в секунду, `SLOW_CALLBACK_DETECTOR=false`,
  метрики, алерт на heartbeat.
- ⬜ 3.4 Модерация: бан игрока, откат сделки, команда владельца, правила в
  закрепе группы.
- ⬜ 3.5 Приёмка: `docs/release-checklist.md` целиком, прогон сессии со
  скринридером, нагрузка 100 игроков, сутки аптайма.
- ⬜ 3.6 Открытие: пост в канале со ссылкой на бота и группу.

## Риски

- ⛔ Без `GROUP_ID` пункты 2.2–2.5 не проверяются на живой группе; код и тесты
  готовы, нужен только ID и права администратора у бота (удаление сообщений).
- Эскроу держит сторону автора, но не цели: цель платит в момент согласия, и
  между проверкой и списанием золота остаётся тот же зазор, что и везде в игре.
- Пошлина 5 процентов не проверена живой экономикой: цифру придётся править по
  первым суткам ОБТ, менять её — одна константа в `economy.py`.
- Баланс арены не проверяется тестами — нужен живой прогон 20 боёв.
- Переименование контента (1.4) ломает сохранённых персонажей: делать миграцией
  идентификаторов, а не переименованием ключей.
